# 第7轮任务卡（主助手）

日期：2026-04-06  
范围：在已可用基础上继续补齐核心体验，不改核心业务逻辑。

## 本轮目标

1. 新增独立“设置页”，把端口修改从首页弹窗迁移到设置页。
2. 首页增加服务状态显示（运行中/已停止）和一键启停按钮。
3. 保持已有能力可用：分享导入、局域网访问、二维码页、通知动作。

## 子助手分工

- 子助手A：设置页最小交互流程建议（小白可理解）。
- 子助手B：服务状态源设计（如何判断运行/停止最稳）。
- 子助手C：WebService 增加启停状态流的实现建议。
- 子助手D：第7轮最小回归清单。

## 通过标准

1. 首页可清晰看到“运行中/已停止”，并能一键切换。
2. 设置页改端口后，返回首页地址同步为新端口并可访问。
3. `:app:assembleDebug` 和 `:app:testDebugUnitTest` 通过。
