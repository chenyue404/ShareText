[主助手回报]
轮次：第2轮
本轮范围：修网页文件、补后端接口、统一服务启停、完成构建检查
通过的改动：

- 修复 `main.html` 中文乱码和脚本错误，保留新增/删除/清空/离线兜底能力
- 后端新增 `/ws` 通道，支持 `add/delete/clear` 消息并广播更新
- 后端根路径 `/` 改为返回 `assets/main.html`
- 服务启停从 `MainActivity` 内嵌服务器迁移到 `WebService`
- Manifest 去掉不匹配的前台服务类型声明
- 增加 Ktor WebSocket 依赖并构建通过
  暂缓的改动：
- 真机联调和网络实测
- Android 宿主层状态模型进一步整理（例如 ViewModel 化）
  结论：通过
  严重问题：
- 暂无本轮阻塞问题
  下一轮入口：
- 做真机端到端联调
- 补充异常场景和稳定性回归
- 继续清理宿主层结构
