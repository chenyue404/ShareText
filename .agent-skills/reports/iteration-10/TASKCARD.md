# 第10轮任务卡

范围冻结：

- 可改：端口占用提示逻辑、可测试性抽取、单元测试。
- 不可改：文本同步链路、增删清逻辑、服务启动主流程。

风险分级：

- 严重问题：改动导致服务无法启动或状态错乱。
- 一般问题：提示文案不一致、测试覆盖不足。

分工表（主助手内部分工）：

- 子任务A：抽取端口建议文案组件（PortAdvice）。
- 子任务B：接入 WebService 与 SettingsActivity。
- 子任务C：补充自动回归单测并执行验收。

验收标准：

- `assembleDebug` 通过
- `testDebugUnitTest` 通过
- 无 P0/P1 遗留
