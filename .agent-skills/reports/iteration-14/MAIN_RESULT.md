[主助手回报]
轮次：14
本轮范围：App+网页 UI 骨架升级
通过的改动：

- 首页改为“状态卡 + 主操作 + 输入卡 + 列表卡”结构。
- 设置页改为卡片分层，保留建议端口一键采用。
- 二维码页改为信息卡+二维码卡，并新增“复制访问地址”。
- 网页 main.html 改为现代卡片布局，新增连接状态显示、空状态、发送失败可见提示。
- 验收通过：`./gradlew :app:assembleDebug`、`./gradlew :app:testDebugUnitTest`。
  暂缓的改动：
- 统一动效细节与深色模式适配。
  结论：通过
  严重问题：
- 无
  下一轮入口：
- UI细节打磨（按钮层级、间距、字体节奏、响应式细节）。
